<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> | GameMatch</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
</head>
<body class="bg-light"> -->
<%- include('../partials/Header') %>

    <div class="container mb-4">
        <div id="post" class="postInfo px-5 py-5 text-center">
            <p class="pt-5 mb-0"><a class="gameLink" href="/games/<%= post.game._id %>"><%= post.game.name %></a> | Tags:
                <% post.tags.forEach(tag => { %>
                    <span><%=tag.name%></span>
                <% }); %></p>
            <h1 class="postTitle mb-3"><%= post.title %></h1>
            <small>Posted by <%= post.author.username %></small>
            <p class="mt-1 pb-4"><%= post.description %></p>
        </div>
        <div class="commentSection pb-2">
            <% if (user !== undefined) { %>
                <div id="new-comment" class="commentBox p-4 mt-4 mb-2">
                    <form action="/games/<%= post.game._id %>/posts/<%= post._id %>/comments" method="POST">
                        <div class="form-group">
                            <label for="text" class="form-label pt-2">Comment</label>
                            <textarea name="text" class="form-control" rows="3" required></textarea>
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">Submit</button>
                        </div>
                    </form>
                </div>
            <% } %>
            <% if (post.comments.length != 0) { %>
                <% post.comments.forEach(comment => { %>
                    <div class="card px-4 mb-2">
                        <small><%= comment.author.username %></small>
                        <p><%= comment.text %></p>
                        <hr>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="card px-4 pb-4 mb-3">
                    <p class="text-center mb-0">No comments yet.</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
